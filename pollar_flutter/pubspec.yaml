name: pollar_flutter
description: Flutter SDK for Pollar – custodial Stellar wallet via sdk.api.pollar.xyz.
version: 0.1.0
repository: https://github.com/pollar-xyz/pollar-flutter
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^10.0.0
  flutter_web_auth_2: ^3.0.0
  cryptography: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
